{
  "name": "api",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "generate": "drizzle-kit generate",
    "push": "drizzle-kit push"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "drizzle-orm": "^0.35.2",
    "hono": "^4.6.5"
  },
  "devDependencies": {
    "@types/bun": "^1.1.11",
    "drizzle-kit": "^0.26.2"
  }
}
